module.exports = function (req, res, next) {
  // if (req.method === "PATCH" && req.url === "/group") {
  //   console.log("PATCH member............", req.url);
  //   res.statusCode = 204;
  // }
  next()
}
